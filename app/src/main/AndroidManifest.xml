<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstapp">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/dota"
        android:label="Boombibu App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NoteActivity"></activity>
        <activity android:name=".ChatActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainOrderActivity" />
        <activity android:name=".OrderUpdate" />
        <activity android:name=".OrderDetailActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".Recleview" />
        <activity android:name=".ui" />
        <activity android:name=".RelativeActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>